This is a voting web based on Django.
how to use this project? 
From the command line, cd this directory, then run the following command:

$ python manage.py runserver

then open your browser to Check out this web.